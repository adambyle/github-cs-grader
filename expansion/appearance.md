---
author: Adam Byle
---

The frontend appearance of the app should not have much to do with the current prototype UI dashboard. The design should be simple white/black, with toggleable dark/light theme defaulting to the user's system settings. The interface should be simple and functionally-oriented, reminscent of version control websites like GitLab (not that there is much common functionality).
